# My portfolio

---

Mostly used for showing the stuff I can do. Sometimes for school projects. You can see them in this repository.

Using Cloudflare Pages to host.

## Local Hosting

This website uses wrangler (cloudflare) to host. And this is the line to locally host it:
`npx wrangler pages dev ./public `
